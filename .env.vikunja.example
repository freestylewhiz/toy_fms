# Copy to .env.vikunja.local and keep that file private (gitignored).
# Base URL only; do not append /api/v1. Use the reachable LAN/VPN/HTTPS address.
VIKUNJA_URL=http://192.168.0.172:3456
# Your Vikunja API token, not a GitHub PAT. Leave this template empty.
VIKUNJA_API_TOKEN=
